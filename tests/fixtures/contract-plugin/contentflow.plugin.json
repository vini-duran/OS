{
  "apiVersion": "1",
  "id": "com.contentflow.e2e-contract",
  "name": "Plugin de Contrato E2E",
  "version": "1.0.0",
  "description": "Fixture isolada para validar binding explícito de portas.",
  "author": "ContentFlow Tests",
  "license": "Proprietary",
  "runtime": { "kind": "node", "version": ">=26 <27", "module": "esm" },
  "entrypoint": "handler.mjs",
  "permissions": [],
  "deliveryTypes": ["text"],
  "capabilities": [
    {
      "id": "generate",
      "operator": "IA",
      "instructionUsage": "required",
      "blockTypes": ["CRIAR"],
      "processTypes": ["script"],
      "inputPorts": [
        {
          "key": "content",
          "label": "Contexto",
          "acceptedTypes": ["list", "number"],
          "required": false,
          "multiple": true
        },
        {
          "key": "outline",
          "label": "Outline / estrutura",
          "acceptedTypes": ["list"],
          "required": false
        },
        {
          "key": "sections",
          "label": "Quantidade de blocos",
          "acceptedTypes": ["number"],
          "required": false
        }
      ],
      "outputPorts": [
        { "key": "result", "label": "Resultado", "producedTypes": ["textarea"], "required": true }
      ],
      "acceptedInputTypes": ["list", "number"],
      "producedOutputTypes": ["textarea"],
      "execution": { "mode": "immediate", "defaultTimeoutMs": 30000, "maxConcurrency": 1 },
      "sideEffects": [],
      "cost": { "model": "free", "estimateSupported": false },
      "dataPolicy": { "sendsDataToThirdParties": false },
      "blockConfigSchema": { "type": "object", "additionalProperties": false, "properties": {} },
      "outputSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": { "result": { "type": "string" } },
        "required": ["result"]
      }
    }
  ]
}
