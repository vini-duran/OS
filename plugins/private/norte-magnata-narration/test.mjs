import assert from "node:assert/strict";import {mkdtemp,rm}from "node:fs/promises";import{tmpdir}from"node:os";import path from"node:path";import{execute}from"./handler.mjs";
const out=await mkdtemp(path.join(tmpdir(),"narration-test-"));try{const r=await execute({invocation:{mode:"start"},capabilityId:"generate-audio-and-real-srt",configuration:{simulate:true},inputs:{script:"Teste."}},{signal:new AbortController().signal,getSecret:async()=>"",getOutputPath:n=>path.join(out,n)});assert.equal(r.status,"success");assert.match(r.values.narration_report,/sem chamadas/i);console.log("norte-magnata-narration: ok")}finally{await rm(out,{recursive:true,force:true})}
